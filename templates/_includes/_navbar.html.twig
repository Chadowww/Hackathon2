<!-- Vertical navbar -->
<div class="vertical-nav" id="sidebar">
    <div class="py-4 px-3">
        <div class="media d-flex justify-content-center">
            <a href="/"><img src="{{ asset('/build/images/logo-emause.35e7cdeb.png') }}" alt="" id="ec-img-nav" ></a>
        </div>
        {% if app.user %}
        <div class="nav-item text-light mt-5">
                Bienvenue <br><h2>{{ app.user.firstname }}</h2>
        </div>
        {% endif %}
    </div>

    <ul class="nav my-5 sm-mb_3" id="ec-li-nav">
        <ul class="nav d-flex flex-column" id="ec-nav-link-new">
        
            {% if app.user %}

            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item">
                    <a href="{{ path('app_smartphone_new') }}" class="nav-link text-light font-italic"> <i class="fa-solid fa-plus text-light fa-fw"></i>&nbsp;&nbsp;Nouvelle entrée </a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_smartphone_index') }}" class="nav-link text-light font-italic"> <i class="fa-solid fa-list text-light fa-fw"></i>&nbsp;&nbsp;Index</a>
                </li>
            {% endif %}

            <li class="nav-item">
                <a href="{# {{ path('app_faq') }} #}" class="nav-link text-light font-italic"><i
                            class="fa-solid fa-question text-light fa-fw"></i>&nbsp;&nbsp;FAQ</a>
            </li>
            {% endif %}
        </ul>
        {% if app.user %}
            {% if is_granted("ROLE_ADMIN") and app.user %}
                <li class="nav-item">
                    <a href="/admin" class="nav-link text-light font-italic">
                        <i class="fa-solid fa-gear text-light fa-fw"></i> Administration </a>
                </li>
            {% endif %}
            <li class="nav-item">
                <a href="/logout" class="nav-link text-light font-italic">
                    <i class="fa-solid fa-right-from-bracket text-light fa-fw"></i> Déconnexion </a>
            </li>
        {% else %}
        <li class="nav-item">
            <a href="/login" class="nav-link text-light font-italic">
                <i class="fa-solid fa-right-to-bracket text-light fa-fw"></i> Connexion</a>
        </li>
        {% endif %}
    </ul>
</div><!-- End vertical navbar -->

<div class="page-content p-3" id="content">
    <!-- Toggle button -->
    <button id="sidebarCollapse" type="button" class="btn btn-light bg-white shadow-sm px-4 mb-4">
        <i class="fa fa-bars"></i></button>
</div>
